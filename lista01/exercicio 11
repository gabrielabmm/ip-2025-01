programa {
  funcao inicio() {
  real numInt

    escreva ("Escreva um número inteiro \n")
    leia (numInt)

    se (numInt % 3 == 0 e numInt % 5 == 0) {
    escreva ("O NÚMERO É DIVISÍVEL \n")
    
    }

    senao escreva ("O NÚMERO NÃO É DIVISÍVEL \n")

  }
}
